NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=

# Server-only. Must match INTAKE_WEBHOOK_SECRET on the backend.
# No NEXT_PUBLIC_ prefix on purpose: this must never reach the browser.
INTAKE_WEBHOOK_SECRET=
